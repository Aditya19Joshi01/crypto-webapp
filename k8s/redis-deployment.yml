apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypto-redis
  labels:
    app: crypto-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crypto-redis
  template:
    metadata:
      labels:
        app: crypto-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
